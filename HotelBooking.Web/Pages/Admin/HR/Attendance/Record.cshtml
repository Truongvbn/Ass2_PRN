@page
@model HotelBooking.Web.Pages.Admin.HR.Attendance.RecordModel
@{
    ViewData["Title"] = "Admin — Record Attendance";
}

<div class="page-container">
    <div class="page-header">
        <a href="/Admin/HR/Attendance/Index" class="back-link"><i class="fas fa-arrow-left"></i> Attendance</a>
        <h1><i class="fas fa-clipboard"></i> Record Attendance</h1>
    </div>

    <div class="form-card">
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger">@Model.ErrorMessage</div>
        }

        <form method="post">
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="Input.HotelId">Hotel</label>
                    <select asp-for="Input.HotelId" class="form-control">
                        @foreach (var h in Model.Hotels)
                        {
                            <option value="@h.Id">@h.Name</option>
                        }
                    </select>
                    <span asp-validation-for="Input.HotelId" class="field-error"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Input.EmployeeId">Employee</label>
                    <select asp-for="Input.EmployeeId" class="form-control">
                        @foreach (var e in Model.Employees)
                        {
                            <option value="@e.Id">@e.FullName</option>
                        }
                    </select>
                    <span asp-validation-for="Input.EmployeeId" class="field-error"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Input.ShiftDate">Shift date</label>
                <input asp-for="Input.ShiftDate" type="date" class="form-control" />
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="Input.WorkShiftId">Work shift (optional)</label>
                    <select asp-for="Input.WorkShiftId" class="form-control">
                        <option value="">Auto / Any</option>
                        @foreach (var s in Model.Shifts)
                        {
                            <option value="@s.Id">@s.Name (@s.StartTime-@s.EndTime)</option>
                        }
                    </select>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="Input.CheckInTime">Check-in time</label>
                    <input asp-for="Input.CheckInTime" type="datetime-local" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="Input.CheckOutTime">Check-out time</label>
                    <input asp-for="Input.CheckOutTime" type="datetime-local" class="form-control" />
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Input.Notes">Notes</label>
                <input asp-for="Input.Notes" class="form-control" />
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Save</button>
                <a href="/Admin/HR/Attendance/Index" class="btn btn-outline">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }

